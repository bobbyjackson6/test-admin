<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin</title>
  </head>
  <body>
    <style>
      body {
        width: 30%;
        min-width: 300px;
        height: 250px;
        position: absolute;
        top: 20%;
        left: 0;
        bottom: 50%;
        right: 0;
        overflow: auto;
        margin: auto;
        border: 2px solid #6ca3d9;
        border-radius: 10px;
      }
      form {
        width: 70%;
        min-width: 300px;
        margin: 0 auto;
      }
      h1 {
        text-align: center;
      }
      label {
        font-size: 24px;
        font-weight: bold;
        text-transform: capitalize;
        margin-right: 20px;
        width: 200px;
      }
      input {
        width: 100%;
        height: 40px;
      }
      .wrapper {
        border-radius: 5px;
        align-items: center;
        display: flex;
        justify-content: space-between;
        border: 3px solid #529fcd;
        margin-bottom: 5px;
      }
      .error{
        text-align: right;
      }
    </style>
    <h1>Log in</h1>
    <div class="error"><%= error.loginMessage %> </div>
    <form method="POST" action="/shimbabumba">
      <div class="wrapper">
        <label for="name">
          name
        </label>
        <input name="login" type="text" placeholder="Login" required/>
      </div>
      <div class="wrapper">
        <label for="name">
          Password
        </label>
        <input name="password" type="password" placeholder="Password" required/>
      </div>
      <input type="submit" value="Confirm" />
    </form>
  </body>
</html>
